🔹 Role:

This is where you write the real logic of your system

👉 It answers:

“What should happen when the user does X?”
How it fits in flow
Route → Controller → Service → Model (DB)

*wait for the responses coming from the model*
